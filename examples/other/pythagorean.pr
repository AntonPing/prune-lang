function pythagorean_triple(a: Int, b: Int, c: Int) -> Unit
begin
    guard a > 0;
    guard b > 0;
    guard c > 0;
    guard a * a + b * b = c * c;
    ()
end

query pythagorean_triple(depth_step=1, depth_limit=1, answer_limit=1)